import DocsLayout from 'layouts/docs';
import Loading from 'components/loading';

# Loading component

<Loading visible className="bg-gray-500 h-24 w-24" />

export default ({ children }) => <DocsLayout>{children}</DocsLayout>