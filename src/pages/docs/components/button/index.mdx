import DocsLayout from 'layouts/docs';
import Button from 'components/ui/button';

# Button component

From [shadcn button](https://ui.shadcn.com/docs/components/button)

<Button onClick={() => {}}>Primary</Button>
<Button variant="secondary" onClick={() => {}}>Secondary</Button>

export default ({ children }) => <DocsLayout>{children}</DocsLayout>
