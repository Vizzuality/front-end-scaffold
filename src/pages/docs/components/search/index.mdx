import DocsLayout from 'layouts/docs';
import Search from 'components/search';

# Search component

<Search theme="light" placeholder="Search for something..." size="sm"/>

export default ({ children }) => <DocsLayout>{children}</DocsLayout>